{% comment %}
  Publications Logos Section
{% endcomment %}

{% assign template = product.metafields.custom.product_template.value %}
{% assign publications = template.pr_mentions.value %}

{% if publications != blank and publications.size > 0 %}
  <hr {% unless block.settings.show_in_quickbuy %}class="not-in-quickbuy"{% endunless %} {{ block.shopify_attributes }}>
  <div class="publications-section">
    <h2 class="h4 align-center">As Featured In</h2>

    <div class="publications-grid">
      {% for publication in publications %}
        {% if publication.logo_image != blank and publication.url_to_publication != blank %}
          <a href="{{ publication.url_to_publication.value.url }}"
             class="publication-link"
             target="_blank"
             rel="noopener noreferrer"
             title="{{ publication.title | default: 'Publication' }}">
            <img
              src="{{ publication.logo_image | image_url: width: 200 }}"
              alt="{{ publication.title | default: 'Publication logo' }}"
              loading="lazy"
              class="publication-logo">
          </a>
        {% endif %}
      {% endfor %}
    </div>
  </div>
{% endif %}